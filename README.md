# SPHY - Einstein Okabe Simbiotic Framework

This repository presents a deterministic reality simulator designed to eliminate the "Uncertainty of Heisenberg" through immutable data structures and cryptographic signatures. By merging General Relativity (Spacetime Curvature) with high-performance data engineering, we demonstrate that "God does not play dice; He draws in Parquet files."

## 🌌 The Framework Vision

The **SPHY Framework** (Simbiotic Physics) is an ontological tool built to prove that reality is auditable, signed, and verifiable. It consists of two primary components:

1.  **The Architect (`sphy_spt_parquet.py`):** A generator that weaves the fabric of spacetime into a Parquet dataset.
2.  **The Auditor (`sphy_spt_visualizer.py`):** A real-time 3D environment that validates the integrity of every single frame against its SHA-256 signature.

---

## 🛠️ What You Will Find in the Visualizer

The Visualizer is the "Tribunal of Facts." When you run the script, you are entering an interactive 3D environment with the following features:

### 1. Einsteinian Spacetime Fabric
*   **Dynamic Curvature:** A 3D grid representing the spacetime metric that physically deforms in the presence of the "Truth Sphere."
*   **Visual Friction:** Deformations are rendered with dynamic color gradients (HSB), mapping gravitational potential into visible light frequencies.

### 2. The Truth Sphere (The Core)
*   **The Being:** A golden wireframe sphere representing the core of the simulation.
*   **Point of Pressure:** You will observe a "pressure point" moving across the sphere, simulating external aggression or arbitration. This movement is not random; it is a signed trajectory.

### 3. Real-Time SHA-256 Audit Panel
*   **Blockchain Integrity:** The top of the screen features a technical HUD (Heads-Up Display) that re-calculates the hash of the current frame and compares it to the stored signature.
*   **Integrity Status:** A live status bar indicates `VALIDATED` (Green) or `CORRUPTED` (Red). If a single bit in the Parquet file is modified by a "Nihilist Agent," the system triggers a reality-corruption glitch.
*   **Continuous Audit Log:** A rolling log in the corner provides a history of the last validated hashes, proving that the past is as immutable as the present.

### 4. Interactive Navigation
*   **3D Rotation:** Left-click and drag to orbit around the spacetime fabric.
*   **Pan/Move:** Right-click and drag to reposition the reality monitor.
*   **Zoom:** Use the mouse wheel to dive into the data vertices.

---

## ⚖️ Philosophical Impact

For the ultra-skeptic, this visualizer is a "Mathematical Exorcism." It removes the comfort of "subjective interpretation" by providing:
*   **Zero Uncertainty:** Every frame is a mathematical certainty.
*   **Auditability:** The source code is open, and the data (Parquet) is raw and accessible.
*   **Deterministic Justice:** The sphere’s resilience proves that the "Truth" always returns to its state of lowest energy (perfection), regardless of the pressure applied.

---

## 🚀 How to Execute

1.  **Generate the Fabric:**
    ```bash
    python sphy_spt_parquet.py
    ```
    *Specify the number of frames (e.g., 1000) to create the immutable dataset.*

2.  **Audit the Reality:**
    ```bash
    python sphy_spt_visualizer.py
    
```
    *Be the observer. Watch as the framework validates each frame of the Einstein-Okabe metric.*

---

> *"Nihilism ends where the SHA-256 hash begins."*
